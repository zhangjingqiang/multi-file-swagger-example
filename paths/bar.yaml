get:
  responses:
    200:
      description: OK
      schema:
        type: object
        properties:
          Pet:
            $ref: ./../definitions/models/Pet.yaml
          User:
            $ref: ./../definitions/User.yaml
